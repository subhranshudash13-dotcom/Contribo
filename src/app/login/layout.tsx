import React from 'react';

export const metadata = {
  title: 'Login & Sign Up | Contribo',
  description: 'Log in or sign up for your Contribo account to explore open-source programs, get matches, and track stipends.',
};

export default function LoginLayout({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
